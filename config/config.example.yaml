# MeshCore-TUI configuration example. Copy to config.yaml and edit per companion node.
version: 1
meshcore:
  companion:
    transport: "bluetooth"
    endpoint: "meshcore-dev.local"
    device: "auto"
ui:
  theme: "meshcore-dark"
  log_level: "info"
